apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: ibm-verify-vault-rolebinding
  namespace: ibm-verify
subjects:
  - kind: ServiceAccount
    name: ibm-verify-sa
    namespace: ibm-verify
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: ibm-verify-vault-role

# Made with Bob
