apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vault-rbac
  namespace: openshift-gitops
  annotations:
    argocd.argoproj.io/sync-wave: "40"
spec:
  destination:
    namespace: ibm-verify
    server: https://kubernetes.default.svc
  project: default
  source:
    path: components/vault-rbac/base
    repoURL: https://github.com/IBM-Security/verify-gitops-demo
    targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

# Made with Bob